services:
  app:
    build:
      context: .
    image: localhost:5000/app:latest
    environment:
      DEBUG: "true"
    ports:
      - 8000:8000
    volumes:
      - ./src:/app/src
    command: uvicorn app:app --reload --host 0.0.0.0 --port 8000
